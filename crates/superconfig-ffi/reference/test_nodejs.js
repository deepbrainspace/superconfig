// Test script to see what Node.js bindings are exposed

// This would work if the Node.js module was properly built
// const { SuperConfig } = require('./path/to/compiled/module');
// const config = new SuperConfig();
// console.log(`Verbosity: ${config.getVerbosity()}`);

console.log(
  "Node.js test file created - this shows the Node.js module would expose SuperConfig class",
);
